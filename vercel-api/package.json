{
  "name": "cursor-dance-api",
  "private": true,
  "type": "module",
  "scripts": {
    "vercel-build": "rm -f api server scripts src && cp -r ../api . && cp -r ../server . && cp -r ../scripts . && cp -r ../src . && echo 'ready'"
  }
}
