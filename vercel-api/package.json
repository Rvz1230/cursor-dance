{
  "name": "cursor-dance-api",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "vercel-build": "rm -rf api server scripts src && cp -r ../api . && cp -r ../server . && cp -r ../scripts . && cp -r ../src . && echo 'ready'"
  }
}
